# /config/vehicles.yaml
# EVCC-Smartload — Vehicle API Configuration
#
# Diese Datei konfiguriert den direkten API-Zugriff auf deine Fahrzeuge.
# Ohne diese Datei werden SoC-Werte nur bei verbundenen Fahrzeugen (via evcc) gelesen.
#
# Beispiel-Konfigurationen für alle unterstützten Fahrzeugtypen:

vehicles:

  # ─────────────────────────────────────────────────────────────────────────
  # KIA / Hyundai / Genesis (via hyundai-kia-connect-api)
  # ─────────────────────────────────────────────────────────────────────────
  - name: KIA_EV9
    type: kia
    username: "deine@email.de"
    password: "deinPasswort"
    pin: "1234"               # optional, je nach Region nötig
    region: 1                 # 1=Europa, 2=Kanada, 3=USA
    brand: 1                  # 1=KIA, 2=Hyundai, 3=Genesis
    # vin: "KNDC..."          # optional, wird auto-erkannt wenn nur 1 Fahrzeug
    capacity_kwh: 99.8
    charge_power_kw: 11       # Wallbox-Ladeleistung in kW

  # ─────────────────────────────────────────────────────────────────────────
  # Renault / Dacia (via renault-api)
  # ─────────────────────────────────────────────────────────────────────────
  - name: my_Twingo
    type: renault
    username: "deine@email.de"
    password: "deinPasswort"
    # vin: "VF1ABC..."        # optional
    locale: "de_DE"           # optional
    capacity_kwh: 22
    charge_power_kw: 7.4

  # ─────────────────────────────────────────────────────────────────────────
  # evcc-only (SoC nur wenn Fahrzeug an Wallbox angesteckt)
  # Für Fahrzeuge ohne Cloud-API (z.B. GWM/ORA wenn evcc SoC liefert)
  # ─────────────────────────────────────────────────────────────────────────
  - name: ora_03
    type: evcc
    capacity_kwh: 63
    charge_power_kw: 6.6

  # ─────────────────────────────────────────────────────────────────────────
  # Custom HTTP-Endpoint (beliebige API)
  # ─────────────────────────────────────────────────────────────────────────
  # - name: mein_auto
  #   type: custom
  #   url: "http://192.168.1.100/api/battery"
  #   soc_path: "battery.level"    # dot-notation JSON-Pfad
  #   # soc_regex: '"soc":\s*(\d+)' # alternativ: Regex
  #   capacity_kwh: 60
  #   charge_power_kw: 11
